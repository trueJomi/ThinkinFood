<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.thinkingFood" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.thinkingFood.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Otros atributos del estilo -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/green_main</item>
        <!-- Desactivar el modo oscuro -->
        <item name="android:colorBackground">@color/white</item>
<!--        <item name="colorSurface">@color/surfaceColor</item>-->
        <item name="materialThemeOverlay">@style/ThemeOverlay.MaterialComponents.Light</item>
    </style>

    <style name="Theme.thinkingFood.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.thinkingFood.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>